export enum Role {
    Nurse = 'Nurse',
    RA = 'RA',
}

export enum Gender {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other',
}

export enum ShiftType {
    Day = 'Day',
    Night = 'Night',
}
